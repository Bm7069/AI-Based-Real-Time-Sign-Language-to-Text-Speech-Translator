

🪄 1. Create and Activate Virtual Environment:::
conda create -n signlang python=3.10 -y
conda activate signlang


📦 2. Install All Required Libraries (in sequence)::::
pip install opencv-python
pip install mediapipe==0.10.21
pip install tensorflow==2.15.0
pip install torch torchvision torchaudio
pip install torchmetrics
pip install scikit-learn==1.3.2
pip install scipy==1.11.4
pip install numpy==1.26.4
pip install matplotlib
pip install tqdm
pip install pandas
pip install pyttsx3
pip install pillow


✅ 3. Collect data (if not done yet)####
python collect_data.py --label A
python collect_data.py --label B
python collect_data.py --label C
python collect_data.py --label D


🧠 4. Train the Model
python train.py

🔧 1. Downgrade ml-dtypes to the compatible version:::
pip install ml-dtypes==0.2.0 --force-reinstall

🔧 2. (Optional, but recommended) Ensure TensorFlow and Mediapipe match correctly::::
pip install tensorflow==2.15.0 mediapipe==0.10.21 --force-reinstall


🔧 3. Verify imports manually (optional)::
python -c "import mediapipe as mp; import tensorflow as tf; print('✅ OK!')"


🧠 4. Run your real-time inference again:::
python infer_realtime.py

pip install pyttsx3
